<p align="right">🌐 언어: <a href="README.md">English</a>  <b>한국어</b></p>


# 노이즈맵 서울 — Python / pgSQL 분석

서울의 구·동별 인기 장소를 고려하여 도시 소음 수준을 시각화하는 인터랙티브 지도를 구축하고, 시간대·교통·날씨·이벤트 등 소음에 영향을 주는 패턴과 요인을 파악합니다.

## 저장소 구성
- **/app/** – 데이터 적재 및 파싱 (pandas → PostgreSQL, PostGIS)
- **/data/raw/** – 월별 XLSX 원천 (시트=측정소)
- **/sql/** – 스키마, 인덱스, 뷰
- **/notebooks/** – 탐색적 분석, 모델링
- **/web/** – 프로토타입 UI (Streamlit / Mapbox)

## 주요 기능 (MVP)
- XLSX → 세로(long) 형태로 변환 (date, hour, station, dB), 시간대 처리(Asia/Seoul → UTC)
- PostGIS 스키마(`stations`, `noise_reading`) 및 upsert
- 기본 분석: 일/주 패턴, 주야간 비교, 상위 소음 지역
- 확장 포인트: 시간별 날씨/교통/이벤트 결합

## 파이프라인
1. **파싱**: 월별 Excel(시트=측정소)  
2. **정규화**: 시간 단위 행 생성 (`date + (hour-1)h`)  
3. **적재**: PostgreSQL에 충돌 안전하게 삽입  
4. **결합(선택)**: 날씨/교통/이벤트

## 데이터 출처
- **소음(주요):** **서울시 열린데이터광장**에서 제공하는 XLSX (2025년 1–6월)  
  데이터셋: *도로교통 소음 측정값(LEQ), 월간·시간대별* — 시트별 측정소 **(시청, 성수, 신촌, 신사)**.  
  파일 헤더에는 “측정월”, “측정일\시간”, 그리고 **1–24시** 컬럼이 포함됩니다.
- 파일은 `data/raw/` 경로에 저장되며, ETL이 각 시트를 `(station, date, hour, dB)` 형태의
  long 포맷으로 변환합니다.
> 참고: 데이터셋 명칭과 측정소 목록은 원본 XLSX를 따르며, 재배포는 제공 기관의 이용약관을
> 따를 수 있습니다. 필요 시 자체 데이터 소스로 대체/보완하세요.

## 데이터베이스 스키마 (public)
- `stations (station_id PK, name UNIQUE, geom geometry(Point,4326) NULL)`  
- `noise_reading (reading_id PK, station_id FK, ts_utc timestamptz, db_level numeric(5,2), part_of_day text, src_month date GENERATED, UNIQUE(station_id, ts_utc))`

## 빠른 시작
```bash
python -m venv .venv && source .venv/bin/activate    # (Windows: .venv\Scripts\Activate.ps1)
pip install -U pip pandas openpyxl SQLAlchemy psycopg2-binary
# app/config.env 설정:
# host=localhost
# port=5432
# user=postgres
# password=YOUR_PASS
# database=noisemap
python app/main.py
```
## 로드맵
날씨/교통/이벤트 결합
인터랙티브 지도 (Streamlit/Mapbox)
예측 & 이상 탐지 (Prophet/ARIMA + 회귀 변수)
CI 및 데이터 품질 대시보드

## 라이선스
MIT (LICENSE 참조)

## 설정
`app/config.env` 파일을 만들거나 수정하고, **비밀번호를 본인 값으로 교체**하세요:

