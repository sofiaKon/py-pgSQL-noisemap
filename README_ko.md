<p align="right">🌐 언어: <a href="README.md">English</a>  <b>한국어</b></p>

# NoiseMap — 교육용 소음 시각화 프로젝트  
---

## 📌 프로젝트 개요

NoiseMap은 **교육 목적**으로 제작된 프로젝트로,  
데이터 전처리, 피크 탐지(일간/글로벌), 시계열 분석, 공간 시각화 기법을 학습하는 데 중점을 두고 있습니다.

프로젝트에 사용된 소음 데이터는 모두 **공개 데이터**이며 `data/raw` 폴더에 저장되어 있습니다.  
소음 지도는 `docs/` 폴더에 정리된 계산 식과 처리 절차를 기반으로 생성됩니다.

생성된 HTML 기반 소음 지도는 **순수 시각적 데모**이며,  
지형 영향, 반사, 기상 조건, 실제 음향 모델 등은 **고려되지 않습니다**.

---

## 🔍 주요 기능

- 원본 소음 데이터(XLSX) 파싱 및 전처리
- PostGIS 기반 스키마 구축 (`stations`, `noise_reading`, `noise_level_d`, `noise_level_h`)
- 일간(day)·글로벌(global) 피크 계산
- 시간(hh) 단위 소음 집계
- HTML 기반 소음 지도 생성
- 전처리된 데이터를 Excel로 내보내기

---

## 🧮 계산 방식

모든 계산식, 가정(assumptions), 데이터 처리 흐름은 다음 문서에 정리되어 있습니다:

➡ **`docs/calculation_methods.md`**

해당 문서에는 다음 내용이 포함됩니다:

- LAeq 계산 방식  
- 일간/전역 피크 산출 방식  
- 시간/일 단위 집계 로직  
- SQL CTE 기반 데이터 변환 과정  
- 전처리(클리닝·정규화) 절차  

---

## 📂 저장소 구성

- **/app/** – 데이터 파싱 및 DB 적재(pandas → PostgreSQL, PostGIS)
- **/data/raw/** – 공개 원본 XLSX 데이터(월별, 측정소 시트별)
- **/docs/** – 계산식 및 전처리 문서
- **/sql/** – 스키마, 인덱스, 뷰 정의
- **/web/** – UI 프로토타입(Streamlit / Mapbox)

---

## 📊 데이터 소스

- **소음 데이터(주요):**  
  2025년 1–6월 월별 XLSX 파일 (서울 열린데이터광장 제공)  
  데이터셋: *도로교통 소음 (LEQ), 시간대별 측정, 측정소 시트 기반*  
  측정소 예시: **시청, 성수, 신촌, 신사**

  XLSX 파일에는 다음과 같은 한국어 열 이름이 포함됩니다:  
  - “측정월”
  - “측정일\시간”
  - 시간 컬럼 **1–24**

- 파일들은 `data/raw/`에 저장됩니다.  
  ETL 파이프라인은 각 시트를 다음 형태의 long-format으로 변환합니다:


> ⚠ 데이터 파일 이름/측정소 구성은 원본 XLSX 규칙을 따릅니다.  
> 재배포는 제공처 약관에 따라 제한될 수 있으므로 필요 시 직접 데이터를 대체하거나 보완하세요.

---

## 🗄 데이터베이스 스키마

스키마 정의는 다음 경로에 있습니다:

➡ **/sql/import.sql**

---

## 🚀 빠른 실행 (Quick Start)

```bash
python -m venv .venv && source .venv/bin/activate
pip install -U pip pandas openpyxl SQLAlchemy psycopg2-binary
python app/main.py
